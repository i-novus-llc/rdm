version: "3.6"
services:
  backend:
    build:
      context: .
      args:
        JAR_FILE: rdm-rest/target/rdm-rest.jar
    image: "538721144237.dkr.ecr.eu-west-1.amazonaws.com/${PROJECT_NAME}_backend:${TAG}"
  frontend:
    build:
      context: .
      args:
        JAR_FILE: rdm-n2o/target/rdm.jar
    image: "538721144237.dkr.ecr.eu-west-1.amazonaws.com/${PROJECT_NAME}_frontend:${TAG}"
