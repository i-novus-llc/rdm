version: "3.6"

services:
  db:
    image: ${DOCKER_REGISTRY}/rdm-database:latest

  security-admin-backend:
    image: ${DOCKER_REGISTRY}/security-admin/backend:4.3

  security-admin-frontend:
    image: ${DOCKER_REGISTRY}/security-admin/frontend:4.3


