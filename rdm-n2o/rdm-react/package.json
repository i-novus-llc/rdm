{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build": "npm run build:config && npm run build:frontend",
    "build:config": "lerna run --scope n2o-config-rdm build",
    "build:frontend": "lerna run --scope rdm-frontend build",
    "publish": "lerna publish from-package --registry https://npm.i-novus.ru"
  },
  "devDependencies": {
    "lerna": "^3.13.2"
  }
}
